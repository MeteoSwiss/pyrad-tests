# Name of this configuration:
# Will be used in product generation save path
name STRING read_process_maxecho_cfradial

# ---------------------------------------------------------
#  Raw data pathes
# ---------------------------------------------------------

# Base path of the raw data.
# Note: Must have a trailing '/'
# ('<datapath>/<scanname>/<YYYY-MM-DD>/<YYYYMMDDHHMMSS00datatype>.<ext>')
datapath STRING ${PYRAD_TESTS_PATH}/input_data/read_process_hydro_cfradial/

# --------------------------------------------
# Output data
# --------------------------------------------
saveimgbasepath STRING ${PYRAD_TESTS_PATH}/pyrad_products_test/
# Supported formats: eps, jpeg, jpg, pdf, pgf, png, ps, raw, rgba, svg, svgz, tif, tiff
imgformat STRING png

# --------------------------------------------------------
# S3 configuration
# --------------------------------------------------------
s3EndpointRead STRING https://eu-central-1.linodeobjects.com/
s3BucketRead STRING pyrad-s3-tests
s3PathRead STRING Cband

# --------------------------------------------------------
# configuration files pathes
# --------------------------------------------------------

# Base path of configuration files
configpath STRING ${PYRAD_TESTS_PATH}/config/

# Configuration of the radar location and approach direction
locationConfigFile STRING ${PYRAD_TESTS_PATH}/config/processing/mch/read_process_maxecho_cfradial_loc.txt

# Configuration of the product generation
productConfigFile STRING ${PYRAD_TESTS_PATH}/config/processing/mch/read_process_maxecho_cfradial_prod.txt

