# MAIN CONFIG FILE
# IMPORTANT: Adapt the paths to the actual location of your data and config files

# Name of this configuration:
# Will be used in product generation save path
name STRING compare_qpe_gauge

# ---------------------------------------------------------
#  Raw data pathes
# ---------------------------------------------------------

# Base path of the raw data.
# Note: Must have a trailing '/'
datapath STRING ${PYRAD_TESTS_PATH}/input_data/compare_qpe_gauge/
path_convention STRING ODIM

# Gauge data
smnpath STRING ${PYRAD_TESTS_PATH}/input_data/compare_qpe_gauge/gauge/

# Loading of saved datasets
loadbasepath STRING STRING ${PYRAD_TESTS_PATH}/pyrad_products_test/
loadname STRING read_display_nexrad

# --------------------------------------------
# Output data
# -----------------------------------------------
# Output image format can be any accepted by Matplotlib
# More than one output formats can be set
saveimgbasepath STRING ${PYRAD_TESTS_PATH}/pyrad_products_test/

imgformat STRARR 1
    png

# --------------------------------------------------------
# S3 configuration
# --------------------------------------------------------
s3EndpointWrite STRING https://eu-central-1.linodeobjects.com/
s3BucketWrite STRING pyrad-s3-tests
s3PathWrite STRING Output


# --------------------------------------------------------
# configuration files pathes
# --------------------------------------------------------

# Base path of configuration files
configpath STRING ${PYRAD_TESTS_PATH}/config/

# Configuration of the radar location and approach direction
locationConfigFile STRING ${PYRAD_TESTS_PATH}/config/processing/base/compare_qpe_gauge_loc.txt

# Configuration of the product generation
productConfigFile STRING ${PYRAD_TESTS_PATH}/config/processing/base/compare_qpe_gauge_prod.txt
